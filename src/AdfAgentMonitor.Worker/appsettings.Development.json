{
  // Local development overrides — never commit real secrets here.
  // Use `dotnet user-secrets set` for ApiKey / ClientSecret / Anthropic:ApiKey.

  "ConnectionStrings": {
    "DefaultConnection": "Server=PLYRFC-PC-0019;Database=AdfAgentMonitor;User Id=adf;Password=adf;TrustServerCertificate=True;"
  },

  "AzureDataFactory": {
    // Leave TenantId / ClientId / ClientSecret empty locally to use DefaultAzureCredential
    // (picks up `az login` or AZURE_* environment variables automatically).
    "LookbackMinutes": 120
  },

  "Email": {
    "SmtpHost": "localhost",
    "SmtpPort": 1025,
    "UseSsl": false,
    "Username": "",
    "Password": "",
    "FromAddress": "adfmonitor@localhost",
    "FromName": "ADF Agent Monitor (Dev)",
    "DashboardBaseUrl": "http://localhost:5071"
  },

  "Hangfire": {
    // Falls back to ConnectionStrings:DefaultConnection — no separate entry needed locally.
    "SqlConnectionString": "",
    "WorkerCount": 2
  },

  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Hangfire": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
