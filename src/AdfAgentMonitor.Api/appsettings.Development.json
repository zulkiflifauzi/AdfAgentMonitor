{
  // Local development overrides — never commit real secrets here.
  // Use `dotnet user-secrets set` for Api:ApiKey / Anthropic:ApiKey / ClientSecret.

  "ConnectionStrings": {
    "DefaultConnection": "Server=PLYRFC-PC-0019;Database=AdfAgentMonitor;User Id=adf;Password=adf;TrustServerCertificate=True;"
  },

  "AzureDataFactory": {
    // Leave TenantId / ClientId / ClientSecret empty locally to use DefaultAzureCredential.
    "LookbackMinutes": 120
  },

  "Hangfire": {
    "SqlConnectionString": "Server=PLYRFC-PC-0019;Database=AdfAgentMonitor;User Id=adf;Password=adf;TrustServerCertificate=True;"
  },

  // CORS — local Blazor WASM development origins.
  // Adjust the ports to match your Dashboard launchSettings.json.
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5000",
      "https://localhost:7000",
      "http://localhost:5071",
      "https://localhost:7071"
    ]
  },

  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
